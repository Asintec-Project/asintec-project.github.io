create table celulares(
    id int auto_increment primary key,
    marca varchar(50) not null,
    ref varchar(50) #referencia como marca samsung y referencia A30
    so varchar(50) #sistema operativo
    

);